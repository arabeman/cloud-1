# Copier en .env puis remplir avec de vraies valeurs.
#   cp .env.example .env
#
# Générer un mot de passe correct :  openssl rand -base64 24

DB_HOST=db
DB_NAME=wordpress
DB_USER=wordpress
DB_PASSWORD=changeme
